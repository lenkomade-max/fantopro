# Scripts Directory

Скрипты для обслуживания и мониторинга FantaProjekt.

## MCP Servers Management

### 🔍 Проверка MCP серверов

```bash
./check-mcp-servers.sh
```

**Что делает:**
- Подсчитывает количество MCP процессов
- Проверяет использование памяти
- Предупреждает о дублях

**Нормальный вывод:**
```
✅ MCP servers count is normal
✅ Memory usage is normal
```

**При проблеме:**
```
⚠️  WARNING: Too many MCP servers detected!
Expected: <40, Found: 76
```

---

### 🧹 Очистка дублей

```bash
sudo ./cleanup-mcp-duplicates.sh
```

**Что делает:**
- Находит активные сессии Claude
- Убивает осиротевшие MCP серверы
- Сохраняет работающие сессии

**⚠️ Требует root прав!**

---

### ⏰ Установка автомониторинга

```bash
sudo ./install-mcp-monitoring.sh
```

**Что делает:**
- Устанавливает cron job (проверка каждый час)
- Создаёт логи в `/var/log/fantaprojekt/mcp-monitoring.log`

**Установить один раз, забыть навсегда!**

---

## Быстрая помощь

### Проблема: Сервер тормозит, swap активен

```bash
# 1. Проверить
./check-mcp-servers.sh

# 2. Очистить
sudo ./cleanup-mcp-duplicates.sh

# 3. Проверить результат
./check-mcp-servers.sh
```

### Проблема: Слишком много процессов

```bash
# Посмотреть сколько MCP процессов
ps aux | grep -E "(npm exec.*mcp)" | wc -l

# Норма: 24-36
# Проблема: >40

# Очистить
sudo ./cleanup-mcp-duplicates.sh
```

### Установить защиту (рекомендуется!)

```bash
# Один раз установить автомониторинг
sudo ./install-mcp-monitoring.sh

# Проверить что установлено
crontab -l | grep check-mcp-servers

# Посмотреть логи
tail -f /var/log/fantaprojekt/mcp-monitoring.log
```

---

## Документация

Полная документация: `../Документы/ЗАЩИТА_ОТ_ДУБЛИРОВАНИЯ_MCP.md`

---

## Другие скрипты (если есть)

*(Место для других скриптов обслуживания)*
