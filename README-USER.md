# 📁 Структура FantaProjekt (24.10.2025)

```
FantaProjekt/
├── src/                    ✅ Исходный код TypeScript
├── static/                 ✅ Музыка, эффекты, фото, шрифты
├── workspace/              ✅ Рабочие файлы (temp, renders, cache, downloads)
├── fantaprojekt-libs/      ✅ Whisper C++ библиотека (1.6GB)
├── Документы/              ✅ Основная документация проекта
│
├── workflows/              📦 n8n workflow JSON файлы
│   ├── v6/                 → 3 файла - AI-enhanced (последняя версия)
│   └── v5/                 → 2 файла - RAG integration
│
├── test-data/              📦 Тестовые JSON запросы (40+ файлов)
│   ├── test-*.json         → Тесты функций
│   ├── CRIME-*.json        → Криминальные workflow
│   └── STRESS-TEST-*.json  → Стресс-тесты производительности
│
├── other-projects/         📦 Другие проекты (не часть основного кода)
│   ├── fanta-site/         → Веб-редактор для видео
│   ├── apps/               → Вспомогательные приложения
│   ├── packages/           → NPM пакеты
│   └── rag-data/           → Данные для RAG системы
│
├── archive/                📦 Старые файлы (не в git)
│   ├── reports/            → 50+ старых отчетов разработки
│   ├── scripts/            → Python/Shell скрипты (устаревшие)
│   ├── screenshots/        → Старые скриншоты
│   └── logs/               → Старые логи
│
└── .ai-temp/               📦 Временные файлы AI-агентов (не в git)
    ├── reports/            → Второстепенные отчеты
    └── scratch/            → Черновики и эксперименты
```

## Где что искать:

**Запуск тестов:**
```bash
curl -X POST http://localhost:3123/api/short-video \
  -H "Content-Type: application/json" \
  -d @test-data/test-advanced-text-overlay.json
```

**Документация:**
- Общая → `README.md`, `CLAUDE.md`
- Техническая → `Документы/АРХИТЕКТУРА.md`, `Документы/МАНИФЕСТ_ПРОЕКТА.md`
- В каждой папке → `README.md` с описанием

**Важные файлы в корне:**
- `package.json` - зависимости
- `docker-compose.yml` - Docker конфигурация
- `cleanup.sh` - скрипт автоочистки
- `.env` - переменные окружения
