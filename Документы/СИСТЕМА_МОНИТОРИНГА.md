# Система мониторинга и уведомлений

**Дата создания:** 24 октября 2025
**Статус:** ✅ Реализовано и протестировано

## Обзор

Система мониторинга автоматически отслеживает состояние сервера и отправляет уведомления в Telegram при критических событиях:
- 🔥 Падение сервера
- 💥 Uncaught exceptions
- ⚠️ Unhandled promise rejections
- 🚀 Запуск сервера
- 🛑 Остановка сервера (SIGTERM/SIGINT)

## Архитектура

### Структура модуля

```
src/monitoring/
├── AlertManager.ts      # Управление уведомлениями в Telegram
├── HealthChecker.ts     # Проверка состояния сервера
├── types.ts             # TypeScript типы
├── index.ts             # Экспорты модуля
├── test-alerts.ts       # Скрипт для тестирования
└── README.md            # Документация модуля
```

### AlertManager

**Основные функции:**
- `sendAlert(data)` - отправка произвольного уведомления
- `sendServerStarted()` - уведомление о запуске
- `sendServerCrashed(error)` - уведомление о падении
- `sendUncaughtException(error)` - uncaught exception
- `sendUnhandledRejection(reason)` - unhandled promise rejection
- `sendTestAlert()` - тестовое уведомление

**Защита от спама:**
- Cooldown 60 секунд между одинаковыми уведомлениями
- Предотвращает дублирование сообщений

**Формат уведомлений:**
```
🚨 CRITICAL - FantaProjekt API (hostname:3123)

🕐 Время: 24.10.2025, 15:30:45
📝 Сообщение:
Сервер упал! Требуется немедленное вмешательство!

❌ Ошибка:
```
Database connection timeout
```

📊 Контекст:
• pid: 12345
• uptime: 45 минут

💻 Система:
• CPU: Intel Core i7
• RAM: 16GB (free: 8GB)
• Uptime: 24h
```

### HealthChecker

**Проверки:**
- Использование памяти (%)
- CPU информация
- Uptime сервера
- Статус: healthy / degraded / unhealthy

**Пороги:**
- `< 75%` памяти = healthy
- `75-90%` памяти = degraded
- `> 90%` памяти = unhealthy

**Endpoint:** `GET /health`

**Пример ответа:**
```json
{
  "status": "healthy",
  "uptime": 3600,
  "timestamp": "2025-10-24T15:30:00.000Z",
  "checks": {
    "memory": {
      "used": 8192,
      "total": 16384,
      "percentage": 50.0
    },
    "cpu": {
      "model": "Intel Core i7",
      "cores": 8
    }
  }
}
```

## Настройка

### 1. Переменные окружения

Добавь в `.env`:

```env
# Monitoring & Alerts
MONITORING_ENABLED=true
TELEGRAM_BOT_TOKEN=8352816822:AAFfr1gVENKMqA8tN85DHvf_9TGk-ilHHBo
TELEGRAM_CHAT_ID=969778981
```

### 2. Создание Telegram бота

**Уже настроено! Но если нужно создать нового:**

1. Открой Telegram и найди [@BotFather](https://t.me/botfather)
2. Отправь `/newbot`
3. Следуй инструкциям
4. Скопируй токен в `.env` как `TELEGRAM_BOT_TOKEN`

### 3. Получение Chat ID

**Уже настроено! Но если нужно узнать свой:**

1. Найди [@get_id_bot](https://t.me/get_id_bot) в Telegram
2. Отправь `/start`
3. Бот пришлет твой Chat ID
4. Добавь его в `.env` как `TELEGRAM_CHAT_ID`

## Использование

### Тестирование системы

```bash
npx ts-node src/monitoring/test-alerts.ts
```

Ты должен получить 2 тестовых сообщения в Telegram.

### Запуск сервера с мониторингом

```bash
npm start
```

При запуске ты получишь уведомление:
```
ℹ️ INFO - FantaProjekt API

✅ Сервер успешно запущен
• port: 3123
• nodeVersion: v18.19.1
• platform: linux
```

### Health Check

```bash
curl http://localhost:3123/health
```

### Кастомные уведомления

В коде можно отправлять произвольные уведомления:

```typescript
import { AlertManager } from './monitoring';

const alertManager = new AlertManager({
  telegramBotToken: process.env.TELEGRAM_BOT_TOKEN,
  telegramChatId: process.env.TELEGRAM_CHAT_ID,
  enabled: true,
  serverName: 'FantaProjekt API',
  port: 3123,
});

// Произвольное уведомление
await alertManager.sendAlert({
  type: 'error',
  message: 'Критическая ошибка в обработке видео',
  error: new Error('FFmpeg timeout'),
  context: {
    videoId: 'abc123',
    duration: 120,
  },
});
```

## Типы уведомлений

| Тип | Эмодзи | Использование |
|-----|--------|---------------|
| `critical` | 🚨 | Падение сервера, критические ошибки |
| `error` | ❌ | Ошибки в работе приложения |
| `warning` | ⚠️ | Предупреждения (высокое использование памяти) |
| `info` | ℹ️ | Информационные сообщения (запуск/остановка) |

## Интеграция

### Process Error Handlers

В `src/index.ts` автоматически настроены обработчики:

```typescript
// Uncaught Exception
process.on('uncaughtException', async (error: Error) => {
  await alertManager.sendUncaughtException(error);
  process.exit(1);
});

// Unhandled Promise Rejection
process.on('unhandledRejection', async (reason: unknown) => {
  await alertManager.sendUnhandledRejection(reason);
});

// Graceful Shutdown
process.on('SIGTERM', async () => {
  await alertManager.sendAlert({
    type: 'warning',
    message: '⚠️ Сервер получил SIGTERM и завершает работу',
  });
  process.exit(0);
});
```

### Health Check Endpoint

В `src/server/server.ts` автоматически добавлен расширенный health check:

```typescript
this.app.get("/health", (_req, res) => {
  if (this.healthChecker) {
    const health = this.healthChecker.getHealthStatus();
    const statusCode = health.status === 'healthy' ? 200 : 503;
    res.status(statusCode).json(health);
  } else {
    res.status(200).json({ status: "ok" });
  }
});
```

## Примеры уведомлений

### Запуск сервера
```
ℹ️ INFO - FantaProjekt API (ubuntu:3123)

🕐 Время: 24.10.2025, 15:30:00
📝 Сообщение:
✅ Сервер успешно запущен

📊 Контекст:
• port: 3123
• nodeVersion: v18.19.1
• platform: linux

💻 Система:
• CPU: Intel Core i7-9700K
• RAM: 16GB (free: 10GB)
• Uptime: 0h
```

### Uncaught Exception
```
🚨 CRITICAL - FantaProjekt API (ubuntu:3123)

🕐 Время: 24.10.2025, 16:45:23
📝 Сообщение:
💥 Uncaught Exception!

❌ Ошибка:
```
Cannot read property 'map' of undefined
```

Stack trace (first 5 lines):
```
TypeError: Cannot read property 'map' of undefined
    at processScenes (/app/src/short-creator/ShortCreator.ts:142:15)
    at async ShortCreator.create (/app/src/short-creator/ShortCreator.ts:89:5)
    at async /app/src/server/routers/rest.ts:45:18
```

💻 Система:
• CPU: Intel Core i7-9700K
• RAM: 16GB (free: 8GB)
• Uptime: 24h
```

## Отключение мониторинга

Если нужно временно отключить:

```env
MONITORING_ENABLED=false
```

Или удали `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` из `.env`.

## Troubleshooting

### Уведомления не приходят

1. Проверь `.env` - все 3 переменные установлены?
2. Проверь токен бота - валиден?
3. Проверь что бот не заблокирован в Telegram
4. Проверь что ты отправил боту хотя бы одно сообщение

### Спам уведомлений

Cooldown система предотвращает дублирование одинаковых сообщений в течение 60 секунд.

### Health check возвращает 503

Это нормально если сервер использует > 90% памяти. Проверь:
```bash
curl http://localhost:3123/health
```

## Зависимости

Модуль использует:
- `node-telegram-bot-api` - для интеграции с Telegram
- `@types/node-telegram-bot-api` - TypeScript типы

Установлены через:
```bash
npm install node-telegram-bot-api @types/node-telegram-bot-api --save --legacy-peer-deps
```

## Дополнительные функции

### Мониторинг в production

Для production окружения рекомендуется:

1. **Logrotate** - ротация логов
2. **PM2** - автоматический перезапуск при падении
3. **Uptime monitoring** - внешний мониторинг доступности

### Docker интеграция

При работе в Docker убедись что переменные окружения прокинуты в контейнер:

```yaml
services:
  fantaprojekt:
    environment:
      - MONITORING_ENABLED=true
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_CHAT_ID=${TELEGRAM_CHAT_ID}
```

## Заключение

Система мониторинга полностью интегрирована в FantaProjekt и готова к использованию. Она автоматически оповестит тебя о любых проблемах с сервером через Telegram.

**Текущий статус:** ✅ Работает
**Тестирование:** ✅ Пройдено
**Production ready:** ✅ Да

---

**Документация:** `src/monitoring/README.md`
**Тестовый скрипт:** `src/monitoring/test-alerts.ts`
**Основной код:** `src/monitoring/AlertManager.ts`
