# Защита от дублирования MCP серверов

**Дата создания:** 24 октября 2025
**Проблема:** Дублирование MCP серверов при перезапуске Claude Code
**Решение:** Автоматический мониторинг и очистка

---

## 📋 Содержание

1. [Как происходит дублирование](#как-происходит-дублирование)
2. [Признаки проблемы](#признаки-проблемы)
3. [Система защиты](#система-защиты)
4. [Использование скриптов](#использование-скриптов)
5. [Превентивные меры](#превентивные-меры)

---

## Как происходит дублирование

### Нормальный процесс запуска

```
1. Cursor IDE открывается
   ↓
2. Claude Code расширение активируется
   ↓
3. Читается конфигурация .mcp.json
   ↓
4. Запускаются MCP серверы (7-8 штук)
   ↓
5. Каждый MCP создаёт 3 процесса:
   - npm exec @package/name
   - sh -c mcp-server-name
   - node /usr/local/bin/mcp-server-name
   ↓
ИТОГО: ~24-27 процессов (нормально!)
```

### Проблемный сценарий

```
1. Что-то повреждает Claude Code расширение
   (чистка сервера, удаление файлов, обновление)
   ↓
2. Cursor перезапускается → расширение НЕ работает
   ↓
3. Пользователь запускает `claude` вручную через терминал
   ↓
4. Создаётся НОВЫЙ набор MCP серверов (24-27 процессов)
   ↓
5. При открытии нового терминала → ещё +24-27 процессов
   ↓
6. Старые MCP серверы НЕ убиваются автоматически!
   ↓
ИТОГО: 50-100+ процессов, 1-2 GB RAM! ❌
```

### Причины дублирования

1. **Множественные сессии Claude Code**
   - Каждая сессия запускает свой набор MCP серверов
   - При закрытии терминала MCP серверы остаются висеть

2. **Запуск через разные методы**
   - Расширение Cursor
   - Команда `claude` в терминале
   - Множественные терминалы

3. **Отсутствие автоочистки**
   - MCP серверы не привязаны к родительскому процессу
   - Не убиваются при закрытии терминала

---

## Признаки проблемы

### 🔴 Критические симптомы

```bash
# Проверить количество MCP процессов
ps aux | grep -E "(npm exec.*mcp|mcp-server-|context7-mcp)" | wc -l
# Норма: 24-36 (1-2 сессии)
# Проблема: >40
```

```bash
# Проверить использование памяти
ps aux | grep -E "(npm exec.*mcp)" | awk '{sum+=$6} END {print sum/1024 " MB"}'
# Норма: 300-600 MB
# Проблема: >800 MB
```

```bash
# Проверить общую память системы
free -h
# Если used > 6 GB и swap активно используется → проблема!
```

### Визуальные признаки

- ❌ Система тормозит
- ❌ Swap активно используется (>2 GB)
- ❌ Docker контейнеры медленно работают
- ❌ Claude Code долго отвечает

---

## Система защиты

Создано **3 скрипта** для автоматической защиты:

### 1. Мониторинг (check-mcp-servers.sh)

**Что делает:**
- Подсчитывает MCP процессы
- Проверяет использование памяти
- Предупреждает о дублях
- Показывает группы процессов по времени старта

**Где находится:**
```
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh
```

**Использование:**
```bash
# Ручная проверка
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh

# Вывод при норме:
# ✅ MCP servers count is normal
# ✅ Memory usage is normal

# Вывод при проблеме:
# ⚠️  WARNING: Too many MCP servers detected!
# Expected: <40, Found: 76
```

---

### 2. Автоочистка (cleanup-mcp-duplicates.sh)

**Что делает:**
- Находит активные сессии Claude
- Определяет их дочерние MCP процессы
- Убивает осиротевшие/старые MCP серверы
- Сохраняет активные сессии

**Где находится:**
```
/home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh
```

**Использование:**
```bash
# Требует root прав!
sudo /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh

# Вывод:
# === MCP Servers Auto-Cleanup ===
# Step 1: Finding active Claude sessions...
# Step 2: Finding old background Claude processes...
# Step 3: Finding all MCP server processes...
# Step 4: Identifying MCP servers to keep...
# Step 5: Killing orphaned MCP servers...
#
# === Cleanup Summary ===
# Processes killed: 42
# Remaining:
# - MCP servers: 24
# - Claude sessions: 1
# ✅ Cleanup successful!
```

**⚠️ Важно:**
- Скрипт **НЕ трогает** активные терминалы с `claude`
- Убивает только осиротевшие и старые (>1 день) процессы
- Безопасен для запуска в любое время

---

### 3. Установка автомониторинга (install-mcp-monitoring.sh)

**Что делает:**
- Устанавливает cron job для проверки MCP каждый час
- Создаёт директорию для логов
- Настраивает автоматическое логирование

**Где находится:**
```
/home/developer/projects/FantaProjekt/scripts/install-mcp-monitoring.sh
```

**Использование:**
```bash
# Установить автомониторинг (один раз)
sudo /home/developer/projects/FantaProjekt/scripts/install-mcp-monitoring.sh

# Вывод:
# ✅ Cron job installed successfully!
#
# Monitoring schedule:
# - Frequency: Every hour
# - Script: check-mcp-servers.sh
# - Log: /var/log/fantaprojekt/mcp-monitoring.log
```

**После установки:**
- Каждый час автоматически проверяется количество MCP серверов
- Логи пишутся в `/var/log/fantaprojekt/mcp-monitoring.log`
- При обнаружении дублей вы увидите предупреждение в логе

**Просмотр логов:**
```bash
# Последние 50 строк
tail -50 /var/log/fantaprojekt/mcp-monitoring.log

# Следить в реальном времени
tail -f /var/log/fantaprojekt/mcp-monitoring.log
```

---

## Использование скриптов

### Быстрая диагностика

```bash
# 1. Проверить есть ли проблема
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh

# 2. Если есть предупреждение → запустить очистку
sudo /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh

# 3. Проверить что всё исправлено
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh
```

### Установка автомониторинга (рекомендуется!)

```bash
# Один раз установить
sudo /home/developer/projects/FantaProjekt/scripts/install-mcp-monitoring.sh

# После этого система будет автоматически проверяться каждый час
```

### Ежедневная рутина

```bash
# Утром перед работой
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh

# Если есть проблема
sudo /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh
```

---

## Превентивные меры

### ✅ Что делать, чтобы избежать дублей

1. **Не запускать множество сессий Claude одновременно**
   - Используйте 1-2 терминала максимум
   - Закрывайте старые терминалы перед открытием новых

2. **Перезапускать Cursor правильно**
   ```bash
   # Перед перезапуском Cursor убейте все MCP серверы
   sudo /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh

   # Затем перезапустите Cursor
   ```

3. **Использовать расширение Claude Code, а не терминал**
   - Расширение управляет MCP серверами автоматически
   - Терминал → дублирование

4. **Установить автомониторинг**
   ```bash
   sudo /home/developer/projects/FantaProjekt/scripts/install-mcp-monitoring.sh
   ```

5. **Еженедельная очистка**
   ```bash
   # Добавить в cron (каждое воскресенье в 3:00)
   0 3 * * 0 /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh >> /var/log/fantaprojekt/mcp-cleanup.log 2>&1
   ```

### ❌ Что НЕ делать

1. **НЕ запускать `claude` из множества терминалов**
   - Каждый запуск → новый набор MCP серверов

2. **НЕ закрывать терминал силой (Ctrl+D, kill)**
   - MCP серверы остаются висеть
   - Правильно: выйти из `claude` командой `exit` или `/quit`

3. **НЕ удалять файлы расширения Cursor**
   - При чистке сервера не трогать `~/.cursor-server/`

4. **НЕ игнорировать предупреждения о памяти**
   - Если Swap > 3 GB → немедленно запустить cleanup

---

## Диагностика проблем

### Если cleanup не помогает

```bash
# 1. Проверить какие процессы остались
ps aux | grep -E "(npm exec.*mcp|mcp-server-|context7)" | grep -v grep

# 2. Убить вручную все MCP серверы (ОСТОРОЖНО!)
ps aux | grep -E "(npm exec.*mcp|mcp-server-|context7)" | awk '{print $2}' | xargs kill

# 3. Убить все Claude процессы
pkill -9 claude

# 4. Перезапустить Cursor
```

### Если система всё равно тормозит

```bash
# Проверить общую память
free -h

# Проверить топ процессов по памяти
ps aux --sort=-%mem | head -20

# Возможно проблема НЕ в MCP серверах, а в:
# - FantaProjekt (рендерит видео)
# - n8n (тяжёлые workflow)
# - Docker (много контейнеров)
```

---

## Мониторинг в реальном времени

### Telegram уведомления (опционально)

Можно добавить в скрипт `check-mcp-servers.sh` отправку уведомлений в Telegram:

```bash
# В конце check-mcp-servers.sh добавить:
if [ "$MCP_COUNT" -gt "$EXPECTED_MAX" ]; then
    # Отправить уведомление в Telegram
    curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
        -d "chat_id=${TELEGRAM_CHAT_ID}" \
        -d "text=⚠️ MCP ALERT: Too many servers ($MCP_COUNT)! Run cleanup." \
        > /dev/null
fi
```

Используйте переменные из `.env`:
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

---

## Заключение

### Основные правила

1. ✅ **Установить автомониторинг** (один раз)
2. ✅ **Проверять перед работой** (check-mcp-servers.sh)
3. ✅ **Очищать при проблемах** (cleanup-mcp-duplicates.sh)
4. ✅ **Использовать 1-2 терминала максимум**
5. ✅ **Правильно закрывать сессии**

### Быстрые команды

```bash
# Проверка
/home/developer/projects/FantaProjekt/scripts/check-mcp-servers.sh

# Очистка
sudo /home/developer/projects/FantaProjekt/scripts/cleanup-mcp-duplicates.sh

# Установка мониторинга
sudo /home/developer/projects/FantaProjekt/scripts/install-mcp-monitoring.sh

# Просмотр логов
tail -f /var/log/fantaprojekt/mcp-monitoring.log
```

### Контакты для помощи

- Документация: `/home/developer/projects/FantaProjekt/Документы/`
- Скрипты: `/home/developer/projects/FantaProjekt/scripts/`
- Логи: `/var/log/fantaprojekt/`

---

**Последнее обновление:** 24 октября 2025
**Версия:** 1.0.0
